{

  "scripts": {
    "test":"cypress run --spec cypress/e2e/1-getting-started/*.js --headless",
    "test1":"cypress run --spec cypress/e2e/2-advanced-examples/*.js --headed"
  },

  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.1",
    "cypress": "^10.8.0",
    "cypress-mochawesome-reporter": "^3.7.0"
  },
  "dependencies": {
    "@cypress/xpath": "^2.0.3"
  }
}
